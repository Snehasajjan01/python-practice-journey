numbers0 = list(map(int,input('Enter the first list elements:').split()))
numbers1 = list(map(int,input('Enter the second list elements:').split()))
merged_list = numbers0 + numbers1
print('After merging two lists:',merged_list)